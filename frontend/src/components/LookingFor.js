function LookingFor(props) {
    return(
        <div className="component" id="looking-for">
            <h3>{props.title}</h3>
            <h2>{props.x} / 100</h2>
        </div>
    );
}
export default LookingFor;